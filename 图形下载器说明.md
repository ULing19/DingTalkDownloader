# 钉钉回放批量下载器（图形界面）

在 [`GoDingtalk`](https://github.com/NAXG/GoDingtalk) 命令行工具之上提供可视化界面，支持：

- **批量粘贴链接**（每行一个，`#` 开头为注释）
- **导入文本文件**（如各科目下的 `链接.txt`）
- **批量导入二维码图片**（自动识别其中的回放链接）
- **任务列表 + 实时进度**（分片百分比、TS→MP4 转换状态）
- **总进度条**、日志、停止下载、重新登录

## 启动方式

双击：

```text
启动图形下载器.bat
```

或在本目录执行：

```bash
python gui_downloader.py
```

首次使用若缺少依赖，批处理会自动安装 `requirements-gui.txt`。

## 使用步骤

1. （首次）点击 **重新登录**，用钉钉账号完成 Cookies 授权  
2. 在左侧粘贴回放链接，或：
   - **导入文本**：选择 `链接.txt`
   - **导入二维码**：多选截图/二维码图片
3. 设置保存目录（可按科目分子文件夹，如 `video\英语-强化`）
4. 点击 **开始下载**，右侧查看每条任务进度
5. 完成后点 **打开保存目录** 查看 MP4

## 依赖

| 组件 | 说明 |
|------|------|
| `GoDingtalk_*.exe` | 本目录已有，负责实际下载与解密 |
| `ffmpeg.exe` | 本目录已有，TS 转 MP4 |
| Python 3.9+ | 图形界面 |
| customtkinter / pillow / opencv | 见 `requirements-gui.txt` |

## 说明

- 底层仍调用 GoDingtalk，进度从它的控制台输出解析。
- 二维码需清晰；过小的图会自动放大后再识别。
- Cookies 路径：`.goDingtalkConfig\cookies.json`
- 本项目开源地址：<https://github.com/NAXG/DingTalkDownloader>
