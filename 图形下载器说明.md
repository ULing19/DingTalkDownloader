# 钉钉回放下载器 1.3.2（图形界面）

这是一个面向 Windows 的中文图形界面，用来批量整理你有权访问的钉钉媒体内容。项目不是钉钉官方软件。

项目地址：<https://github.com/ULing19/DingTalkDownloader>

## 支持的链接

- 群直播回放：`n.dingtalk.com/dingding/live-room/...?...roomId=...&liveUuid=...`
- 钉钉闪记：`https://shanji.dingtalk.com/app/transcribes/<转写ID>`
- 群文件/钉盘视频：`https://qr.dingtalk.com/page/yunpan?route=previewDentry&spaceId=<空间ID>&fileId=<文件ID>&type=file`

闪记和群文件适配需要登录，并且账号必须有对应记录或文件的查看/下载权限。群文件视频会走播放流；普通 PDF、Office、图片或压缩包若返回直接下载地址，会按原始扩展名保存，不经过 FFmpeg。只有预览地址、文件夹、权限不足或无媒体地址的条目无法下载。

## 启动方式

安装版：从开始菜单或桌面快捷方式启动 `DingTalkDownloader.exe`。

绿色版：解压 `DingTalkDownloader_1.3.2_Portable.zip` 后双击 `DingTalkDownloader.exe`。以下文件必须在同一目录：

```text
DingTalkDownloader.exe
GoDingtalk_v2.5.2_windows_amd64.exe
mediago.exe
ffmpeg.exe
```

从源码运行：

```powershell
python -m pip install -r requirements-gui.txt
python gui_downloader.py
```

源码运行还需要自行放置 GoDingtalk、MediaGo v0.3.0 Windows amd64 和 FFmpeg。构建脚本会生成 `dist\DingTalkDownloader_1.3.2\`、`DingTalkDownloader_1.3.2_Portable.zip` 和 `DingTalkDownloader_1.3.2_Setup.exe`。

## 使用步骤

1. 点击“重新登录”，用有权限的钉钉账号完成授权。
2. 在左侧粘贴链接，或点击“导入文本”“导入二维码”。每行一个 URL，`#` 开头的行会忽略。
3. 点击“解析到任务列表”，确认链接类型、标题和保存目录。
4. 点击“开始下载”，在右侧查看任务进度、转换状态和日志。
5. 完成后点击“打开保存目录”。默认目录是程序目录下的 `video\`。

### 当前群回放链接一键获取

主程序只保留一个“一键获取已打开群回放”按钮，整合了原客户资料目录中的采集逻辑：

1. 在钉钉登录并打开需要处理的群“直播广场”；多个群可分别打开直播页。
2. 点击输入区下方的“一键获取已打开群回放”。程序会通过登录态只读接口自动读取每个群的全部分页，不需要手动滚动。
3. 选择一次保存根目录，程序按已识别的群名称建立目录并写入各自的 `链接集.txt`。
4. 链接和钉钉回放标题会同时写入左侧输入框与右侧任务列表，下载文件优先使用该标题。

采集器从 CEF 日志和导航信息识别当前群，优先使用登录态只读 LWP RPC 自动翻页；RPC 不可用时才回退读取经过严格校验的渲染进程内存。它不点击群聊、不发送消息，也不执行任何写操作。未打开直播页的群无法凭空读取；群身份无法确认时会提示失败，旧的 `链接集.txt` 不会被覆盖。

直播回放优先由 MediaGo 取得完整 HLS，再交给 FFmpeg 合并为 MP4；MediaGo 不可用时才回退到 GoDingtalk。闪记和钉盘视频根据源媒体返回结果下载。钉盘普通文件若有直接下载地址，则按 MIME/文件名/URL 保留原始扩展名。不同链接即使标题相同，也会自动追加 `(1)`、`(2)` 等编号，不会覆盖已有文件。闪记记录可能只有音频或转写数据，并不保证存在视频画面。

MP4 合并会保留钉钉源 PTS 并从零起算，避免低帧率屏幕共享被误判为时间戳跳变；完成后再检查音视频轨的起止时间。若仍提示“视频轨比音频早结束”，文件会保留，程序不会用 `-shortest` 静默删除较长音频。

## 登录与隐私

登录会话保存在 `.goDingtalkConfig\`，只供本机调用。不要分享 `cookies.json`、浏览器 Cookie 导出文件、密码或含有私密参数的链接。会话过期或权限变化时再次点击“重新登录”。程序不绕过登录、群成员权限、文件下载限制或组织策略。

## 引擎分工

| 引擎 | 用途 | 来源 |
| --- | --- | --- |
| GoDingtalk v2.5.2 | 群直播回放兼容回退 | <https://github.com/NAXG/GoDingtalk> |
| MediaGo v0.3.0 | 群回放原始 HLS、闪记、CSpace/钉盘媒体解析 | <https://github.com/Sophomoresty/mediago/releases/tag/v0.3.0> |
| FFmpeg | HLS/TS 分片合并与容器转换 | <https://ffmpeg.org/> |

MediaGo 仓库标注 Unlicense，但其钉钉实现源码注释提到由反编译的 `Dingtalk_Live_Client.pyc` 移植。该许可不等于第三方来源或平台授权已被确认；使用和再分发前请阅读 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。

## 常见错误

| 现象 | 处理 |
| --- | --- |
| 未找到引擎 | 确认四个 EXE 同目录，检查安全软件隔离记录 |
| 闪记缺少 `account/access_token` 或 `deviceid` | 重新登录，确认同一账号能打开闪记页面 |
| 群文件 URL 不支持 | 升级到 1.3.2，保留 `route`、`spaceId`、`fileId`、`type` 参数 |
| 视频轨比音频早结束 | 文件已保留；先用原链接重试，仍出现时检查钉钉源回放是否也会定格 |
| 没有可下载媒体 | 先确认当前账号能在钉钉客户端打开；再检查转码状态或普通文件直链 |
| 缺少 `roomId/liveUuid` | 不要把闪记/钉盘 URL 当直播 URL，使用完整原始链接 |
| 只有 TS，没有 MP4 | 确认 `ffmpeg.exe` 存在且未被拦截，再重试任务 |
| 二维码识别失败 | 使用清晰原图、保留二维码边缘，或直接粘贴解析出的 URL |
| 当前群链接获取失败 | 确认钉钉已登录并保持目标群直播广场打开；无需滚动末页，首次处理时按提示选择任意已有可写文件夹 |

## 卸载

安装版可从 Windows 设置、开始菜单卸载项或安装目录的卸载程序移除。卸载程序始终保留 `video\` 和 `.goDingtalkConfig\`，需要清理时再手动删除。绿色版退出后删除整个解压目录即可。

## 许可与责任

GUI 自有代码采用 MIT License。GoDingtalk、MediaGo、FFmpeg 和 Python 依赖遵循各自许可证。本工具仅用于学习、研究和个人备份；用户须自行确认内容授权、隐私和版权责任。
